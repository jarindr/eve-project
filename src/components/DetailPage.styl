.prev
  position: absolute
  left: 0
  height: 120%
.next
  position: absolute
  right: 0
  height: 100%
.navigation
  margin-top: 40px
  position: relative
  height: 25px
.sliderContainer
  width: 100%
.imageSlide
  margin: 0 auto
  max-height: 500px
.container
  max-width: 1000px
  width: calc(100% - 200px)
  margin: 0 auto
.detail
  margin 20px 0 50px 0
.imageName
  margin-top: 40px
  text-align: center
.imageWork
  img
    width: 250px
